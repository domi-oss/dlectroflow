# Focus timer audio assets — provenance & license

The focus timer's optional device effects reference bundled audio assets:

| File                | Purpose                                    | Status               |
| ------------------- | ------------------------------------------ | -------------------- |
| `alarm.wav`         | Short chime at time's-up (alarm)           | **Real, audible** ✅ |
| `lofi/*.mp3` (× 10) | Looping lo-fi focus tracks (#43 library)   | **Real, audible** ✅ |

> The MR ② silent placeholder `lofi-calm.mp3` has been **removed** and replaced by
> the real curated lo-fi library under `lofi/` (#43). The `lofi_calm` focus-sound
> value is retained (backward-compat with saved settings) and now maps to
> `lofi/aurora-on-mute.mp3`.

## License

**CC0 1.0 Universal (public domain dedication)** for every file here.

- `alarm.wav` — self-authored, no third-party copyright.
- `lofi/*.mp3` — dedicated to the public domain under CC0 1.0 by their author
  (see provenance below).

## Provenance / status

### `alarm.wav` — a real, audible chime

Synthesized programmatically (a gentle 3× two-tone A5/D6 chime, enveloped to
avoid clicks; 16-bit PCM, 44.1 kHz, mono, ~1.3 s). Self-authored, so the CC0
dedication is accurate and safe to ship.

### `lofi/*.mp3` — the #43 curated lo-fi library

- **Source:** [open-lofi](https://github.com/btahir/open-lofi) by Bilal Tahir —
  "A free, public-domain collection of lo-fi music tracks." Release `v1.0.0`
  (`openlofi.zip`), catalog manifest
  [`catalog.json`](https://raw.githubusercontent.com/btahir/open-lofi/main/catalog.json)
  (`"license": "CC0-1.0"`).
- **License:** **CC0 1.0 Universal.** The repository ships a full CC0 1.0
  `LICENSE`, and its README explicitly dedicates the *audio content itself* to the
  public domain: "All tracks are released under CC0 1.0 Universal — you can copy,
  modify, distribute, and use them for any purpose, including commercial, without
  asking permission or giving credit." No attribution required.
- **Origin caveat (recorded for diligence, not a blocker):** the author states
  the tracks "were all generated by me using Suno v5 and are owned by myself as a
  premium member. I'm now donating them to the public domain." The CC0 dedication
  therefore rests on Suno's premium-tier terms granting the author ownership of
  the generated output — a second-hand link in the provenance chain (the author's
  claim, not independently verifiable by us). We bundle only because the CC0
  dedication is explicit and covers commercial use; this note preserves the chain
  of title for audit.
- **What we bundled:** a curated subset of **10 tracks — one per open-lofi
  category** (the full 166-track / ~544 MB catalog is *not* vendored; streaming it
  is deferred to #61). Fetched from the CC0 release and committed under `lofi/`.
  Total ≈ 29 MB.

| File (`lofi/`)                 | Track title              | open-lofi category |
| ------------------------------ | ------------------------ | ------------------ |
| `aurora-on-mute.mp3`           | Aurora on Mute           | ambient-lofi       |
| `porchlight-golden-hour.mp3`   | Porchlight Golden Hour   | chillhop           |
| `breezy-afternoon-terrace.mp3` | Breezy Afternoon Terrace | jazzhop            |
| `barefoot-in-the-kitchen.mp3`  | Barefoot in the Kitchen  | soul-rnb           |
| `3-am-echoes.mp3`              | 3 AM Echoes              | late-night         |
| `burnt-sunset-groove.mp3`      | Burnt Sunset Groove      | funk-soul          |
| `lanterns-in-slow-motion.mp3`  | Lanterns in Slow Motion  | asian-lofi         |
| `after-school-rain.mp3`        | After School Rain        | seasonal-weather   |
| `chapter-by-lamplight.mp3`     | Chapter By Lamplight     | activities         |
| `cafe-da-tarde.mp3`            | Cafe Da Tarde            | hybrid             |

Streaming sources (YouTube / Spotify / SoundCloud) remain out of scope: the app's
CSP has no `media-src`, so audio can only come from its own origin. The rest of
the open-lofi catalog now reaches the app through that same origin — see below.

## The streamed catalog (#61) — same licence, not in this repo

The other 156 open-lofi tracks are **not vendored here**; the full set is ~544 MB
and a container image is the wrong place for it. An operator who sets
`FOCUS_CATALOG_ORIGIN` points the app at a directory holding the extracted
`openlofi.zip` — the mp3s plus `catalog.json`, which is published separately from
the archive and has to be fetched on its own — and the app reads them at run
time. Unset, everything above is all there is, which is the default.

**The licence is the same, and so is the chain of title.** The streamed set is the
rest of the same open-lofi `v1.0.0` release documented above: CC0 1.0 Universal,
no attribution required, commercial use included, resting on the same
second-hand Suno-ownership claim recorded in the origin caveat. Nothing about the
provenance changes by being fetched rather than committed — but two things about
the *deployment* do, and they are the operator's to get right:

1. **What is in the store is what plays.** The app validates shape, never
   content: it accepts `{title, filename, category}` entries whose filename looks
   like a plain `.mp3` name, and it has no way to know whether the bytes behind
   one are CC0. Upload the open-lofi release, not a mixtape. `catalog.json`'s own
   `"license": "CC0-1.0"` field is a claim by whoever wrote the file, and the app
   does not read it as permission.
2. **The provenance record travels with the store, not with this file.** If you
   serve a different set, the note above stops describing what your instance
   plays, and this file is no longer the licence record for it.

The bundled ten stay bundled regardless. They are the fallback when the store is
unreachable, so a focus session is never silent for want of a network.
