apiVersion: v1
kind: Service
metadata:
  name: dlectroflow
  labels:
    {{- include "dlectroflow.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "dlectroflow.selectorLabels" . | nindent 4 }}
  ports:
    - port: 80
      targetPort: 3000
