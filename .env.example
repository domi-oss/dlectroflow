# dlectroflow environment variables.
# Copy this file to `.env.local` and fill in the values. `.env.local` is
# gitignored — never commit real secrets.
#
#   cp .env.example .env.local
#
# Prisma also reads DATABASE_URL from `.env` (created by `prisma init`); keeping
# it here in .env.local works for `next dev`/`next start` too.

# ── Required ────────────────────────────────────────────────────────────────

# Claude API key (server-side only, never exposed to the browser).
# Get one at https://console.anthropic.com → API keys.
ANTHROPIC_API_KEY=

# Database connection string.
#   Local (default):  file:./dev.db          (SQLite, zero setup)
#   Production:        postgresql://…         (see README → Deploy)
DATABASE_URL="file:./dev.db"

# ── Optional / feature-gated ─────────────────────────────────────────────────

# Reclaim.ai scheduling does NOT use an env token — you connect it at runtime
# from the app via a browser OAuth flow ("Connect Reclaim"), and the tokens are
# stored in the database. Nothing to set here.

# Email round-up (planned) — Resend. Leave blank until the feature ships.
# RESEND_API_KEY=
# ROUNDUP_FROM_EMAIL=
